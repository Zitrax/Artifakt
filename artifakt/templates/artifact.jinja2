<!DOCTYPE html>
<html lang="${request.locale_name}">
<head>
    <title>Artifact</title>
</head>
<body>
    <table>
        {% for col in artifact.__table__.columns %}
            <tr><th>{{ col.name|capitalize }}</th><td>{{ artifact|attr(col.name) }}</td></tr>
        {% endfor %}
        <tr><th>File size</th><td>{{ artifact.size }} ({{ artifact.size_h }})</td></tr>
    </table>
    <form action="/artifact/{{ artifact.sha1 }}/download"><input type="submit" value="Download"></form>
    <form action="/artifact/{{ artifact.sha1 }}/view"><input type="submit" value="View"></form>
    <form action="/artifact/{{ artifact.sha1 }}/delete"><input type="submit" value="&#x1f5d1; Delete"></form>
</body>
</html>