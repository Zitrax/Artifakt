<!DOCTYPE html>
<html lang="{{ request.locale_name }}">
<head>
    {% include 'head.jinja2' %}
    <title>Artifakt - Reset Password</title>
</head>
<body>
{% include 'navbar.jinja2' %}
<div class="container">

    <h1>Recover your password</h1>
    <div class="row-fluid">
        <div class="span2"></div>
        <div class="span8">
            {% if not status %}
                <div class="alert alert-error">Error! ${msg}</div>
            {% endif %}
            <form class="form-horizontal" name="reset_password" method="POST"
                  action="{{ request.current_route_path() }}">
                <fieldset class="form-group">
                    <div class="form-group">
                        <label class="control-label col-sm-2" for="reset[email]">Your e-mail address:</label>
                        <div class="col-sm-10">
                            <input type="email" class="form-control" placeholder="username@hostname.com" name="email"
                                   id="reset[email]"/>
                            <input type="hidden" name="csrf_token" value="{{ csrf_token }}"/>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="submit" class="btn btn-large btn-primary">Reset Password</button>
                    </div>
                </fieldset>
            </form>
        </div>
        <div class="span2"></div>
    </div>

</div>