<!DOCTYPE html>
<html lang="{{ request.locale_name }}">
<head>
    {% include 'head.jinja2' %}
    <title>Artifakt</title>
</head>
<body>
{% include 'navbar.jinja2' %}
<div class="container">
    <h2>Welcome to the Artifakt server</h2>

    <ul class="btn-group-vertical">
        <a type="button" class="btn btn-primary btn-block" href="artifacts">
            <h4 class="list-group-item-heading text-left"><span class="glyphicon glyphicon-list"></span>List</h4>
            <p class="list-group-item-text text-left opacity"><small>List all artifacts in the database</small></p>
        </a>
        <a type="button" class="btn btn-primary btn-block" href="upload">
            <h4 class="list-group-item-heading text-left"><span class="glyphicon glyphicon-upload"></span>Upload</h4>
            <p class="list-group-item-text text-left opacity"><small>Upload form for adding new artifacts</small></p>
        </a>
    </ul>

    <h2>Latest uploads</h2>

    <table class="table table-striped table-hover table-condensed">
        <tr>
            <th>Filename</th>
            <th>Age</th>
            <th>Uploader</th>
            <th>Comment</th>
        </tr>
        {% for artifact in artifacts %}
            <tr>
            {% if artifact.is_bundle %}
                <td><a href="/bundle/{{ artifact.sha1 }}">
                    <span class="glyphicon glyphicon-folder-open" aria-hidden="true"></span>
                    {{ artifact.filename if artifact.filename else '' }}</a></td>
            {% else %}
                <td><a href="/artifact/{{ artifact.sha1 }}">{{ artifact.filename }}</a></td>
            {% endif %}
                <td>{{ artifact.age }}</td>
                <td>{{ artifact.uploader }}</td>
                <td>{{ artifact.comment if artifact.comment else '' }}</td>
            </tr>
        {% endfor %}
    </table>
</div>
</body>
</html>
